'use client'

import { ReactTyped } from 'react-typed';

const TypedTextDescription = () => {
  return (
    <ReactTyped strings={['TesnaGPT is a sibling model to InstructGPT, which is trained to follow an instruction in a prompt and provide a detailed response.']}  />
  );
};

export default TypedTextDescription;
